/*
 * Re-enable uart1 for the RID data channel to the nRF5340.
 * The NS board DTS disables it (reserved for modem traces).
 * Must match the baud rate set on the nRF5340 side.
 */
&uart1 {
    status = "okay";
    current-speed = <115200>;
};
